#pragma once

#ifndef SINTATICO_H

	#define SINTATICO_H

    #include <string>
    #include <stdio.h>
    #include <queue>
    #include "elemento.h"

    bool ProcedimentoASD(queue<Node>);

    bool ProcedimentoCorpo(queue<Node>*);
    
#endif